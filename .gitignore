# Secrets — never commit the API key
.env

# Python junk
venv/
__pycache__/
*.pyc

# Live data — the database is data, not code. It gets uploaded to the
# host once, then lives its own life there. Committing it would overwrite
# server data with laptop data on every deploy.
bomu.db
bomu.db.*
# SQLite scratch files — a journal/WAL pushed to the server can silently
# "roll back" the live database with laptop data (this happened 2026-07-23)
bomu.db-journal
*.db-journal
*.db-wal
*.db-shm

# Scratch photo uploads
uploads/

# check_specs.py --json output. A point-in-time audit report, not source.
spec_flags.json

# sweep_namematch.py --json output. Same reasoning.
sweep_namematch.json

# Design mockups / explorations — reference only, not app code
design_explorations/
